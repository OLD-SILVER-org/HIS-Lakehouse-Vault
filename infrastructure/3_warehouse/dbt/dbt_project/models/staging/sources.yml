version: 2

sources:
  - name: staging
    database: "{{ env_var('WAREHOUSE_POSTGRES_DB') }}"
    schema: public # Hoặc schema chứa các bảng staging từ Spark đổ vào
    tables:
      - name: dm_benh_nhan
      - name: dm_nhan_vien
      - name: dm_dich_vu
